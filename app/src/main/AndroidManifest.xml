<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="umn.useit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UseIt">

        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_detail"
            android:theme="@style/Theme.UseIt.NoActionBar" />

        <activity
            android:name=".prehome.SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".prehome.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <activity
            android:name=".prehome.SignUpActivity"
            android:label="Sign Up"
            android:screenOrientation="portrait" />

        <activity
            android:name=".prehome.SignInActivity"
            android:label="Sign In"
            android:screenOrientation="portrait" />

        <activity
            android:name=".home.DashboardActivity"
            android:theme="@style/Theme.UseIt.NoActionBar" />


        <activity
            android:name=".ChatActivity"
            android:theme="@style/Theme.UseIt.NoActionBar" />

        <activity android:name=".NotificationActivity"
            android:label="Notifications"/>

        <activity
            android:name=".SucceedActivity"
            android:theme="@style/Theme.UseIt.NoActionBar" />

    </application>
</manifest>